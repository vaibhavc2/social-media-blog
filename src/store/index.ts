import {
  RootState,
  AppDispatch,
  useAppDispatch,
  useAppSelector
} from "./store";
import { store } from "./store";

export type { RootState, AppDispatch };

export { useAppDispatch, useAppSelector, store };
